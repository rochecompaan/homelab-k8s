apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postiz-uploads
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
