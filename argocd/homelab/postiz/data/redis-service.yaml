apiVersion: v1
kind: Service
metadata:
  name: postiz-redis
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: postiz-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
