apiVersion: v1
kind: Service
metadata:
  name: wyoming-piper
spec:
  type: ClusterIP
  selector:
    app: wyoming-piper
  ports:
    - name: wyoming
      port: 10200
      targetPort: wyoming
      protocol: TCP
