apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piraeus-rwx-controlled-pvc-run-001
  namespace: storage-benchmark-rwx
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app.kubernetes.io/name: storage-benchmark-rwx
    storage.compaan.io/backend: piraeus-rwx-controlled
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: piraeus-rwx-controlled-bench-3r
  resources:
    requests:
      storage: 20Gi
